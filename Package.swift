import PackageDescription

let package = Package(
	name: "DScrollView",
	platforms: [
		.iOS(.v13)
	],
	products: [
		.library(
			name: "DScrollView",
			targets: ["DScrollView"]),
	],
	dependencies: [
		
	]
)
